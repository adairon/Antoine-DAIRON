{% extends 'base.html.twig' %}

{% block title %}Modifier {{ musee.nom }}{% endblock %}

{% block body %}

    <div class="container">

    <div class="card bg-warning col-9 mx-auto my-3">
        <img class="card-img-top img-edit rounded my-1" src="{{ musee.photo }}" alt="Card image cap">
        <div class="card-body">
            <h1 class="card-title text-white ">Modifier {{ musee.nom }}</h1>
        </div>
    </div>

        <div class="col-9 justify-content-center m-auto border border-warning rounded p-3">

            {{ include('musee/_form.html.twig', {'button_label': 'Modifier'}) }}

        </div>
        <div class="d-flex justify-content-around my-5">
            <button class="btn btn-secondary">
                <a href="{{ path('musee_index') }}">retour à la liste</a>
            </button>
            {{ include('musee/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
