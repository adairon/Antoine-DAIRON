{% extends 'base.html.twig' %}

{% block title %}Modifier {{ musee.nom }}{% endblock %}

{% block body %}

    <div class="container">
    <h1 class="col-9 bg-warning justify-content-center mx-auto my-5 py-3 rounded text-white ">Modifier le {{ musee.nom }}</h1>
        <div class="col-9 justify-content-center m-auto border border-warning rounded p-3">

            {{ include('musee/_form.html.twig', {'button_label': 'Modifier'}) }}

        </div>
        <div class="d-flex justify-content-around my-5">
            <button class="btn btn-secondary">
                <a href="{{ path('musee_index') }}">retour à la liste</a>
            </button>
            {{ include('musee/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
