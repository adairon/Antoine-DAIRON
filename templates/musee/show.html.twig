{% extends 'base.html.twig' %}

{% block title %}{{ musee.nom }}{% endblock %}

{% block body %}
    <h1>{{ musee.nom }}</h1>

    <div class="container">

    <div class="card mb-3">
        <img class="card-img-top" src="{{ musee.photo }}" alt="Card image cap">
        <div class="card-body">
            <h3 class="card-title">{{ musee.nom }}</h3>
            <p class="card-text"><small class="text-muted">adresse : {{ musee.adresse }}</small></p>
            <p class="card-text"><small class="text-muted">prix : {{ musee.prix }} </small></p>
            <p class="card-text">{{ musee.presentation }}</p>
            <p class="card-text">{{ musee.oeuvre }}</p>
        </div>
        <div class="card-footer d-flex justify-content-around">
            <button class="btn btn-secondary">
                <a href="{{ path('musee_index') }}">retour à la liste</a>
            </button>

            <button class="btn btn-warning">
                <a href="{{ path('musee_edit', {'id': musee.id}) }}">Modifier</a>
            </button>
        </div>
    </div>
    <div class="d-flex justify-content-center my-5">
        {{ include('musee/_delete_form.html.twig') }}
    </div>

        {# <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ musee.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ musee.nom }}</td>
                </tr>
                <tr>
                    <th>Photo</th>
                    <td><img src="{{ musee.photo }}" alt="{{ musee.nom }}"></td>
                </tr>
                <tr>
                    <th>Adresse</th>
                    <td>{{ musee.adresse }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ musee.prix }}</td>
                </tr>
                <tr>
                    <th>Presentation</th>
                    <td>{{ musee.presentation }}</td>
                </tr>
                <tr>
                    <th>Oeuvre</th>
                    <td>{{ musee.oeuvre }}</td>
                </tr>
            </tbody>
        </table>

        <button class="btn btn-secondary my-3">
            <a href="{{ path('musee_index') }}">retour à la liste</a>
        </button>

        <button class="btn btn-warning">
            <a href="{{ path('musee_edit', {'id': musee.id}) }}">Modifier</a>
        </button>

        {{ include('musee/_delete_form.html.twig') }}
    </div> #}



{% endblock %}
